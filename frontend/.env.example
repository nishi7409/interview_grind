# Frontend configuration (optional)
# In dev, /api is proxied to the backend by vite.config.js (VITE_API_TARGET).
# In production, set the absolute API base URL the built app should call:
# VITE_API_BASE=https://your-api.example.com/api
